apiVersion: v1
kind: Service
metadata:
  name: nestjs
  namespace: nestjs
  labels:
    app: nestjs
spec:
  type: ClusterIP
  selector:
    app: nestjs
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
